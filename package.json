{
  "name": "tnt-maritime-intelligence",
  "version": "1.1.0",
  "description": "TNT Global Maritime Commodity Intelligence — secured multi-user portal",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-concurrency=1",
    "seed": "node scripts/seed-users.js",
    "seed:force": "node scripts/seed-users.js --force",
    "setup": "npm install && node scripts/seed-users.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "uuid": "^11.0.5"
  }
}
